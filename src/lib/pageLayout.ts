/** Shared horizontal rhythm with Figma-style ~1280px frame + gutters */
export const PAGE_SHELL =
  "max-w-7xl mx-auto w-full px-6 md:px-10 lg:px-12";

/** Vertical padding for stacked home sections (inner `PAGE_SHELL` wrapper). */
export const SECTION_PAD_Y = "py-14 md:py-16 lg:py-20";

/** Hero strip around the framed video — tighter bottom so the next section isn’t separated by a large gap. */
export const HERO_OUTER_PAD =
  "pt-9 pb-4 md:pt-10 md:pb-5 lg:pt-11 lg:pb-5";
